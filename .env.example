# Copy to .env and fill in real values. .env is gitignored; this file is tracked.

# Django
SECRET_KEY=your-secret-key-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# PostgreSQL
DB_NAME=mp_management
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# PRP API (Members-of-Parliament data source) — used by import_mp_api / Sync button
PRP_API_USER=
PRP_API_PASS=
